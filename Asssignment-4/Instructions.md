P4: Write a program in Python to implement Best First Search (BFS) and Beam Search algorithm for graph / tree.

• Take user input graph, heuristic value for each node and beam width (in case of beam search).
• User input source node and goal node.
• Show traversal path and all intermediate steps in both case of BFS and Beam search for comparison.